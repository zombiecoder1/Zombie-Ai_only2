1. Execution Policy

কোনো ধাপে demo response বা test run তৈরি করা যাবে না।

অপ্রয়োজনীয় ফাইল/ফোল্ডার (test file, bat file, dummy folder) তৈরি করা যাবে না।

প্রতিটি কাজ সরাসরি main project flow তে প্রয়োগ করতে হবে।

2. Server & Logging

প্রতিটি সার্ভার run করার পরে অবশ্যই লগ ফাইল চেক করতে হবে।

লগ ফাইল স্বয়ংক্রিয়ভাবে তৈরি হলে সেখান থেকেই error/issue track করতে হবে।

যদি লগে error আসে, তখনই সংশোধন করতে হবে – skip করা যাবে না।

3. Prompt → Model → Output Flow

Prompt থেকে মডেল সঠিকভাবে ডেটা রিসিভ করতে হবে।

Output অবশ্যই:

Workable হতে হবে (server/project এ চলবে)

Industry best practice অনুসরণ করবে

Output code/documentation শর্টকাট বা bypass দিয়ে দেওয়া যাবে না।

4. UI/UX & Functional Update

ইউজার যা বলবে তাই সঠিকভাবে UI/Backend এ প্রয়োগ করতে হবে।

জটিল করে উপস্থাপন করা যাবে না – সহজ ভাষায়, সহজ ডিজাইনে করতে হবে।

5. Validation

প্রতিটি ধাপে user requirement cross-check করতে হবে।

“assumption” করে কাজ করা যাবে না, শুধুমাত্র requirement অনুযায়ী।

🎯 Example Workflow (ডেমো নয়, ব্যাখ্যা হিসেবে)

ইউজার prompt দিল: “নতুন কলাম যোগ করো”

Agent → সরাসরি DB migrate script modify করবে

Server run করবে → লগ চেক করবে

Error থাকলে সাথে সাথে ঠিক করবে

ইউজার বলল: “UI তে suggestive search চাই”

Agent → View + JS modify করবে

User flow অনুযায়ী implement করবে

Server + browser log check করবে

এভাবে Cursor AI Agent এই rules strictly follow করলে:

bypass হবে না

test/demo জিনিস আসবে না

server log ভিত্তিক কাজ হবে

output industry standard থাকবে